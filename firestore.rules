rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Starter-deny policy: do not open room data until server functions
    // and authenticated membership checks are implemented and tested.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
